<!DOCTYPE html>
<html>

	<head>
		<title>Dustin Webb Photography</title>
		<%= stylesheet_link_tag    "application", :media => "all" %>
		<%= javascript_include_tag "application" %>
		<%= csrf_meta_tags %>
		<%= render 'layouts/shim' %>
		<%= yield :head %>
	</head>
	
	<body>
		<%= render 'layouts/header' %>

		<div class="main-container">
		
			<%= yield %>

			<%= render 'layouts/footer' %>
		</div>
		
		

	</body>

	
</html>